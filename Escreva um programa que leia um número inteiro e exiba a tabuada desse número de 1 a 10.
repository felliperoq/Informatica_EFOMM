program tabuada;
var num, i: integer;

begin
    writeln('Digite um número');
    readln(num);
    for i:=1 to 10 do
    begin
        write(num, ' x ', i, ' = ', num*i);
        writeln('');
    end;
end.
